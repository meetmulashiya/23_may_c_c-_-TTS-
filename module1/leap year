#include<stdio.h>
#include<conio.h>
void main()
{
	int y;
	clrscr();
	printf("Enter a year:");
	scanf("%d",&y);
	if(y%400==0 || y%4==0 && y%100!=0)
	{
		printf("%d\n is a leap year");
	}
	else
	{
		printf("%d\n is not a leap year");
	}
	getch();
}
